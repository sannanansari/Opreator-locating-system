import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Operator-Locating-system',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
